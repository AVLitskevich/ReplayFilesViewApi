services:
  replay-viewer:
    build: .
    container_name: replay-viewer
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ${ROOT_PATH}/replay-data/appsettings.json:/app/appsettings.json:ro
      - ${ROOT_PATH}:/app/gamedata:ro
