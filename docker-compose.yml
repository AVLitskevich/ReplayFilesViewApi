services:
  replay-viewer:
    build: .
    container_name: replay-viewer
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ~/replay-data/replays:/app/replays
      - ~/replay-data/appsettings.json:/app/appsettings.json:ro
